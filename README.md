# TOO LONG; DIDN'T READ
Copy and paste below command into terminal (LINUX & DEBIAN & BASH CMD ONLY. Windows soon?).
```
sudo apt update && sudo apt upgrade; curl https://sh.rustup.rs -sSf | sh; cargo install --git https://github.com/alexheretic/ab-av1; sudo apt update && sudo apt upgrade; sudo apt autoremove ffmpeg; sudo add-apt-repository ppa:savoury1/ffmpeg4 -y; sudo add-apt-repository ppa:savoury1/ffmpeg5 -y; sudo apt update && sudo apt upgrade; sudo apt install ffmpeg; ffmpeg -version; git clone https://gitlab.com/AOMediaCodec/SVT-AV1.git; sudo apt install gcc cmake yasm clang; ~/SVT-AV1/Build/linux/build.sh release; cp ~/SVT-AV1/Bin/Release/SvtAv1EncApp ~/; chmod u+x SvtAv1EncApp; clear; echo "You are almost done! For this last step, I need you to do it by yourself... After this, a nano .bashrc editor will be opened. I need you to go to the end of the line (navigate using arrow key), make a new line, and paste this command: 'export PATH=~/:$PATH' (exclude the quotation mark!). To save the file, press [CTRL + X], then [CTRL + Y], then press [ENTER]. Please, I need you to get this right."; read -p "Understood? Press any key to continue..."; nano .bashrc; rm -rf ~/SVT-AV1; clear; echo "That's it! Program has been installed! Happy VMAFing! Need some guides? Do refer to https://alexheretic.github.io/posts/ab-av1/";
```

# TABLE OF CONTENTS


# 1.0 Introduction
